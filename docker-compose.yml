services:
  codex-sub-proxy:
    build: .
    image: codex-sub-proxy:local
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
      HOST: 0.0.0.0
    restart: unless-stopped
